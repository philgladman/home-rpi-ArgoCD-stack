apiVersion: v1
kind: Service
metadata:
  labels:
    app: mattermost-postgresql
  name: mattermost-postgresql
spec:
  selector:
    app: mattermost-postgresql
  type: ClusterIP
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
    name: postgresql
