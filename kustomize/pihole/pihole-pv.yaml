apiVersion: v1
kind: PersistentVolume
metadata:
  name: pihole-pihole-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /volumes/ssd-1/nas-vol
